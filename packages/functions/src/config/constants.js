const CREDIT_COSTS = {
    yanAvatar: 1,
    yanDraw: 1,
    yanPhotobooth: 1,
};

const PRODUCT_IDS = Object.keys(CREDIT_COSTS);

const PRODUCTS = [...PRODUCT_IDS];

module.exports = {
    CREDIT_COSTS,
    PRODUCT_IDS,
    PRODUCTS,
};
